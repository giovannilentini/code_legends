<section class="flex flex-col md:flex-row items-center justify-center min-h-screen bg-gradient-to-br from-gray-900 via-gray-800 to-gray-700">
  <div class="flex flex-col md:flex-row w-full max-w-7xl p-10 space-y-12 md:space-y-0 md:space-x-10">

    <!-- Sezione Informazioni Utente -->
    <div class="w-full md:w-1/3 bg-gray-800 p-8 rounded-3xl shadow-2xl flex flex-col items-center transform hover:scale-105 transition duration-300">
      <div class="text-center mb-6">
        <h2 class="text-5xl font-extrabold text-white tracking-tight drop-shadow-lg">Profilo Utente</h2>
      </div>
      <div class="text-center mb-6">
        <% if current_user.profile_image.attached? %>
          <%= image_tag current_user.profile_image_thumbnail, class: "rounded-full mx-auto shadow-xl w-40 h-40 object-cover border-4 border-indigo-500" %>
        <% else %>
          <%= image_tag('profile-placeholder.png', alt: 'Profile Image', class: 'rounded-full mx-auto shadow-xl w-40 h-40 object-cover border-4 border-indigo-500') %>
        <% end %>
      </div>
      <div class="text-left w-full space-y-6">
        <h3 class="text-3xl font-bold text-indigo-400">Informazioni Utente</h3>
        <p class="text-lg text-gray-300"><strong>Nome:</strong> <%= current_user.username %></p>
        <p class="text-lg text-gray-300"><strong>Email:</strong> <%= current_user.email %></p>
      </div>

      <!-- Sezione Lista Amici -->
      <div class="mt-8 w-full">
        <h3 class="text-3xl font-bold text-indigo-400 mb-4">Amici</h3>
        <ul class="list-disc list-inside space-y-2 text-gray-300">
          <% current_user.friends.each do |friend| %>
            <li><%= link_to friend.username, user_profile_path(friend), class: 'text-blue-400 hover:text-blue-500' %></li>
          <% end %>
          <% current_user.inverse_friends.each do |friend| %>
            <li><%= link_to friend.username, user_profile_path(friend), class: 'text-blue-400 hover:text-blue-500' %></li>
          <% end %>
        </ul>
      </div>

      <!-- Pulsante per accedere alle impostazioni -->
      <div class="mt-8 text-center w-full">
        <%= link_to 'Impostazioni Profilo', edit_settings_path, class: 'bg-gradient-to-r from-green-500 to-green-700 hover:from-green-600 hover:to-green-800 text-white font-bold py-3 px-8 rounded-full shadow-lg transform hover:scale-105 transition duration-300' %>
      </div>
    </div>

    <!-- Sezione Proposte Utente -->
    <div class="w-full md:w-2/3 space-y-20">

      <!-- Proposte Accettate -->
      <div>
        <h3 class="text-4xl font-extrabold mb-8 text-green-400 tracking-tight drop-shadow-md">Proposte Accettate</h3>
        <div class="overflow-hidden rounded-2xl shadow-lg bg-gray-800 transform hover:scale-105 transition duration-300">
          <table class="w-full border border-gray-700 rounded-2xl">
            <thead>
            <tr class="bg-gray-900">
              <th class="px-6 py-4 text-center text-xl font-semibold text-white">Descrizione</th>
              <th class="px-6 py-4 text-center text-xl font-semibold text-white">Test Case</th>
            </tr>
            </thead>
            <tbody>
            <% @accepted_challenges.each do |challenge| %>
              <tr class="border-b border-gray-700 hover:bg-gray-700 transition-colors duration-200">
                <td class="px-6 py-4 text-white"><%= challenge.description %></td>
                <td class="px-6 py-4 text-white">
                  <ul class="list-disc list-inside space-y-2">
                    <% challenge.test_cases.each do |test_case| %>
                      <li>Input: <%= test_case.input_example %>, Output: <%= test_case.expected_output %></li>
                    <% end %>
                  </ul>
                </td>
              </tr>
            <% end %>
            </tbody>
          </table>
        </div>
      </div>

      <!-- Proposte Rifiutate -->
      <div>
        <h3 class="text-4xl font-extrabold mb-8 text-red-400 tracking-tight drop-shadow-md">Proposte Rifiutate</h3>
        <div class="overflow-hidden rounded-2xl shadow-lg bg-gray-800 transform hover:scale-105 transition duration-300">
          <table class="w-full border border-gray-700 rounded-2xl">
            <thead>
            <tr class="bg-gray-900">
              <th class="px-6 py-4 text-center text-xl font-semibold text-white">Descrizione</th>
              <th class="px-6 py-4 text-center text-xl font-semibold text-white">Test Case</th>
              <th class="px-6 py-4 text-center text-xl font-semibold text-white">Motivo del Rifiuto</th>
            </tr>
            </thead>
            <tbody>
            <% @rejected_challenges.each do |challenge| %>
              <tr class="border-b border-gray-700 hover:bg-gray-700 transition-colors duration-200">
                <td class="px-6 py-4 text-white"><%= challenge.description %></td>
                <td class="px-6 py-4 text-white">
                  <ul class="list-disc list-inside space-y-2">
                    <% challenge.test_cases.each do |test_case| %>
                      <li>Input: <%= test_case.input_example %>, Output: <%= test_case.expected_output %></li>
                    <% end %>
                  </ul>
                </td>
                <td class="px-6 py-4 text-white"><%= challenge.rejection_reason %></td>
              </tr>
            <% end %>
            </tbody>
          </table>
        </div>
      </div>

      <!-- Bottone per aprire la casella postale -->
      <div class="text-center w-full">
        <button id="openMailbox" class="bg-gradient-to-r from-blue-500 to-blue-700 hover:from-blue-600 hover:to-blue-800 text-white font-bold py-3 px-8 rounded-full shadow-lg transform hover:scale-105 transition duration-300">
          Casella Postale
        </button>
      </div>

      <!-- Torna alla Home -->
      <div class="mt-16 text-center">
        <%= link_to 'Torna alla Home', play_now_path, class: 'bg-gradient-to-r from-blue-500 to-blue-700 hover:from-blue-600 hover:to-blue-800 text-white font-bold py-3 px-10 rounded-full shadow-lg transform hover:scale-105 transition duration-300' %>
      </div>
    </div>

  </div>

  <!-- Finestra Modale per le richieste di amicizia -->
  <div id="mailboxModal" class="fixed inset-0 flex items-center justify-center z-50 hidden">
    <div class="bg-gray-800 p-8 rounded-3xl shadow-2xl w-full max-w-2xl">
      <div class="flex justify-between items-center mb-4">
        <h3 class="text-3xl font-bold text-indigo-400">Richieste di Amicizia</h3>
        <button id="closeMailbox" class="text-gray-400 hover:text-gray-300">&times;</button>
      </div>
      <ul class="space-y-4 text-white">
        <% if @friend_requests.present? %>
          <% @friend_requests.each do |request| %>
            <li>
              <a href="#" class="text-blue-400 hover:text-blue-500" data-request-id="<%= request.id %>">
                Richiesta da <%= request.sender.username %>
              </a>
            </li>
          <% end %>
        <% else %>
          <li class="text-gray-400">Nessuna richiesta di amicizia in sospeso.</li>
        <% end %>
      </ul>
    </div>
  </div>

  <!-- Finestra Modale per Visualizzare la Richiesta -->
  <div id="requestModal" class="fixed inset-0 flex items-center justify-center z-50 hidden">
    <div class="bg-gray-800 p-8 rounded-3xl shadow-2xl w-full max-w-2xl">
      <div class="flex justify-between items-center mb-4">
        <h3 class="text-3xl font-bold text-indigo-400">Richiesta di Amicizia</h3>
        <button id="closeRequest" class="text-gray-400 hover:text-gray-300">&times;</button>
      </div>
      <p class="text-white mb-6" id="requestMessage"></p>
      <div class="flex justify-between">
        <%= form_with url: '#', method: :post, id: 'acceptRequestForm', class: 'inline' do %>
          <%= button_tag 'Accetta', class: 'bg-green-500 hover:bg-green-700 text-white font-bold py-3 px-6 rounded-full' %>
        <% end %>
        <%= form_with url: '#', method: :post, id: 'rejectRequestForm', class: 'inline' do %>
          <%= button_tag 'Rifiuta', class: 'bg-red-500 hover:bg-red-700 text-white font-bold py-3 px-6 rounded-full' %>
        <% end %>
      </div>
    </div>
  </div>

</section>

<script>
      document.getElementById('openMailbox').addEventListener('click', function () {
          document.getElementById('mailboxModal').classList.remove('hidden');
      });

      document.getElementById('closeMailbox').addEventListener('click', function () {
          document.getElementById('mailboxModal').classList.add('hidden');
      });

      document.querySelectorAll('#mailboxModal a').forEach(function (el) {
          el.addEventListener('click', function (e) {
              e.preventDefault();
              const requestId = this.getAttribute('data-request-id');

              const senderName = this.innerText.split(' ')[2];
              document.getElementById('requestMessage').innerText = `Ciao, sono ${senderName}, vuoi diventare mio amico?`;

              document.getElementById('requestModal').classList.remove('hidden');

              const acceptForm = document.getElementById('acceptRequestForm');
              const rejectForm = document.getElementById('rejectRequestForm');

              acceptForm.action = `/friend_requests/${requestId}/accept`;
              rejectForm.action = `/friend_requests/${requestId}/reject`;
          });
      });

      document.getElementById('closeRequest').addEventListener('click', function () {
          document.getElementById('requestModal').classList.add('hidden');
      });
</script>
